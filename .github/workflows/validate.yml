name: validate

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: package metadata and files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Validate repository
        run: sh scripts/validate.sh
